<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>useable v{{ version }}</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Archivo:wght@400;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: Archivo, sans-serif;
    }
    h1 .navbar-text {
      font-size: 0.7em;
    }
    h2 {
      font-size: 1.8rem;
    }
    h3 {
      font-size: 1.5rem;
    }
    h4 {
      font-size: 1.25rem;
      margin-top: 1.5em;
    }
    h2.anchor:before, h3.anchor:before {
      height: 4rem;
      content: "";
      display:block;
    }
    .list-group.contents {
      max-width: 30rem;
      margin-left: 1rem;
    }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top mb-3">
  <div class="container-fluid">
    <a class="visually-hidden-focusable nav-item" href="#main">Skip to Main Content</a>    
    <h1 class="navbar-brand ms-3 mt-1">
      <a href="#" class="text-black"><b>use</b>able</a>
      <span class="navbar-text">v{{ version }}</span>
    </h1>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <span class="visually-hidden">Usage</span> Categories
          </a>
          <ul class="dropdown-menu">
            {% for cat in taxonomy['categories'] %}
            <li>
              <a class="dropdown-item" href="#{{ cat.name }}">{{ cat.name }}</a>
            </li>
            {% endfor %}
          </ul>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Download <span class="visually-hidden">taxonomy as a file</span>
          </a>
          <ul class="dropdown-menu">
            <li>
              <a class="dropdown-item" href="./useable.json" download>as JSON</a>
            </li>
          </ul>
        </li>
        <li class="nav-item">
          <a class="nav-link icon-link" href="{{ metadata.repository }}"><img class="bi" src="./assets/github-mark.svg" aria-hidden="true"><span class="visually-hidden"> GitHub</span> Repository</a>
        </li>
      </ul>
    </div>
  </div>
</nav>

<div class="container-md">
  <h2 class="anchor" id="main">Overview</h2>
  <p><b>use</b>able, coordinated by <a href="https://www.accessiblecommunity.org">Accessible Community</a>, {{ metadata['description'] }}</p>
  <p> The <b>use</b>able taxonomy is based around previous work in the fields of accessibility and accommodations:
    <ul>
    {% for ref in metadata['references'] %}
      <li><a href="{{ ref.url }}">{{ ref.name }}</a> provided by the {{ ref.org }}.</li>
    {% endfor %}
    </ul>
  </p>
  <p>We <a href="{{ metadata.repository }}/issues">welcome comments and feedback</a> to encourage the establishment of a worldwide mapping to help build tools and solutions for people with disabilities. Please create or comment on any of the current GitHub issues.</p>
  <p>If you'd prefer, you can download the structure of the taxonomy as a JSON document by clicking the download dropdown in the navbar at the top.</p>

  <h2 class="mt-5">Usage categories</h2>
  <div class="mb-3">
    <div class="list-group contents">
    {% for cat in taxonomy['categories'] %}
      <a class="list-group-item list-group-item-action" href="#{{ cat.name }}">{{ cat.name }}</a>
    {% endfor %}
    </div>
  </div>

{% for cat in taxonomy['categories'] %}
  <div>
    <h2 id="{{ cat.name }}" class="anchor">{{ cat.name }}</h2>
    {% if cat.description %}<p>{{ cat.description }}</p>{% endif %}
    <h3>Requirements for use</h3>
    <div class="list-group contents">
      {% for req in cat['requirements'] %}
      <a href="#{{ cat.name }}-{{loop.index}}" class="list-group-item list-group-item-action">{{ req.name }}</a>
      {% endfor %}
    </div>
    {% for req in cat['requirements'] %}
    <div>
      <h3 id="{{ cat.name }}-{{loop.index}}" class="anchor">{{ req.name }}</h3>
      {% if req.description %}<p>{{ req.description }}</p>{% endif %}
      <h4>Associated conditions and disabilities</h4>
      <ul> <!-- class="list-group list-group-flush"> -->
        {% for condition in req['conditions'] %}
          <li>{{ condition }}</li>
        {% endfor %}
      </ul>
    </div>
    {% endfor %}
  </div>
{% endfor %}
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>

</body>
</html>